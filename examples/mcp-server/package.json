{
  "name": "example-mcp-server",
  "version": "0.0.1",
  "private": true,
  "description": "agent-identity example: MCP server exposing credential resolution as tools",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "start:http": "node server.js --transport http"
  },
  "dependencies": {
    "@datacules/agent-identity": "^0.4.0",
    "@datacules/agent-identity-mcp": "^0.2.0",
    "@datacules/agent-identity-audit": "^0.2.0"
  }
}
