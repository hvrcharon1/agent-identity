/**
 * Re-exports credential router from @datacules/agent-identity (packages/core).
 */
export {
  MemoryCredentialStore,
  CredentialRouter,
  createRouter,
  createRouterFromStore,
} from '@datacules/agent-identity';
