{
  "name": "agent-identity-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/stores/*",
    "packages/integrations/*"
  ],
  "scripts": {
    "dev": "turbo dev --filter=@datacules/agent-identity-app",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "turbo": "^1.13.3"
  },
  "engines": {
    "node": ">=20"
  }
}
